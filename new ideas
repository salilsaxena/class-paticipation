A menu driven program would be nice but try mergeing the similar functions in a same menu, and make a go back menu for seprate menus 
Make a function for length related function in `main.c` 
insert all the sub menu functions in `void main` within a switch case and switch inside a `while(1)` loop
