started adding `int no_col` to head 
  check it in insert_del.h
  check del_col 
  del_col can be used for single columns also...

buffer works fine

segration is done,test it more.

tag named `before_float` is made which contains totally safe build, revert back to it OR dont mere if found in any kind of confusion.
